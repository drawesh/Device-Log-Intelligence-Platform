# Device Log Intelligence Platform - Render requirements (SQLite)
#
# Render config in render.yaml uses SQLite:
#   DATABASE_URL=sqlite:///./device_logs.db
#
# Therefore we intentionally omit psycopg2/psycopg2-binary to avoid build failures
# (psycopg2 may fall back to source build and requires pg_config).

# FastAPI and server
fastapi==0.109.0
uvicorn[standard]==0.27.0
python-multipart==0.0.6

# Database (SQLite)
sqlalchemy==2.0.25

# JSON logging
python-json-logger==2.0.7

# Utilities
pydantic==2.6.4
pydantic-settings==2.2.1

# ---
# Render build fix: ensure a prebuilt pydantic-core wheel is available for the target Python.
# Pinning keeps deployment from compiling Rust extensions (maturin/pydantic-core).
pydantic-core==2.16.3

# ---
# IMPORTANT: Render uses linux wheels; pydantic-core should come from wheels.
# If Render ever attempts to build from source, pin/upgrade pydantic-core to a wheel-available version.

# CORS
starlette==0.35.1

