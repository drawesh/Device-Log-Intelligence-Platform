 # Device Log Intelligence Platform - Requirements

# FastAPI and server
fastapi==0.109.0
uvicorn[standard]==0.27.0
python-multipart==0.0.6

# Database
sqlalchemy==2.0.25
psycopg2-binary==2.9.9
# psycopg2-binary must use prebuilt wheels; if not available in the build environment, deployment fails.
# (Render uses Linux where wheels are available; this comment is documentation.)


# JSON logging
python-json-logger==2.0.7

# Utilities
pydantic==2.6.4
pydantic-settings==2.2.1

# ---
# Render build fix: ensure a prebuilt pydantic-core wheel is available for the target Python.
# Pinning keeps deployment from compiling Rust extensions (maturin/pydantic-core) when
# Render uses a newer Python version.
pydantic-core==2.16.3

# CORS
starlette==0.35.1
 